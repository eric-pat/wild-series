<form method="post" action="{{ path('app_season_delete', {'slug': season.slug}) }}"
      onsubmit="return confirm('Etes vous sur de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
