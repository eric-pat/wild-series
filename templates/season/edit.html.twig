{% extends 'base.html.twig' %}

{% block title %}Edit Season{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center my-5">
        Modifier {{ season.program.title }} {{ season.slug }}
    </h1>

    {{ include('season/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex justify-content-center">
        <div class="d-flex justify-content-between col-md-6">
            <a href="{{ path('app_season_index') }}">
                <button class="btn  btn-success">
                    back to list
                </button>
            </a>

            {{ include('season/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
