<form method="post" action="{{ path('program_delete', {'slug': program.slug}) }}"
      onsubmit="return confirm('Etes vous sur de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ program.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
