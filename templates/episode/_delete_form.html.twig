<form method="post" action="{{ path('episode_delete', {'slug': episode.slug}) }}"
      onsubmit="return confirm('Êtes vous sur de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ episode.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
